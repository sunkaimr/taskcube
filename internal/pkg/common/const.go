package common

import "math"

const InvalidUint uint = math.MaxUint
const InvalidInt int = math.MaxInt

const (
	RequestID = "RequestID"
	JWT       = "JWT"
	LOGGER    = "LOGGER"
	DB        = "DB"

	AdminUser = "admin"
)

const ReservedVersions = 20
